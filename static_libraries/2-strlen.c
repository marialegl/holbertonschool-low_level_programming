#include "main.h"
#include <string.h>

/**
 * _strlen - returns the length of a string.
 * @s: pointer.
 * Return: cantidad de letras.
 */
int _strlen(char *s)
{
	int cantidadletras;

	cantidadletras = strlen(s);
	return (cantidadletras);
}
